{
  "name": "happyfeeling",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev:bot": "docker compose up -d postgres redis && pnpm --filter @happyfeeling/db migrate:deploy && docker compose build web worker && docker compose up -d web worker && docker compose logs -f web worker"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
