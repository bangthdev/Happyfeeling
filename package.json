{
  "name": "happyfeeling",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": ">=18" },
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:bot": "bash scripts/dev-with-ngrok.sh"
  }
}
